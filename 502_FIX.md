# ✅ 502 Bad Gateway Fix

## 🔧 Problem:
Cloudflare zeigt 502 Bad Gateway, weil Frontend nicht auf Port 3000 lief.

## ✅ Lösung:

### 1. Frontend gestartet:
```bash
cd /root/gaestefotos-app-v2/packages/frontend
pnpm dev
```
- ✅ Frontend läuft jetzt auf Port 3000
- ✅ Erreichbar unter `http://localhost:3000`

### 2. Backend Status:
- ✅ Backend läuft bereits auf Port 8001
- ✅ Erreichbar unter `http://localhost:8001/api`

### 3. Nginx Proxy:
- ✅ Konfiguriert für Frontend (Port 3000)
- ✅ Konfiguriert für Backend API (Port 8001)

---

## 📋 Services Status:

```bash
# Frontend prüfen
curl http://localhost:3000

# Backend prüfen  
curl http://localhost:8001/api

# Über HTTPS (Cloudflare)
curl -k https://app.xn--gstefotos-v2a.com/api
```

---

## ⚠️ Wichtig für Production:

Die Services müssen dauerhaft laufen! Aktuell laufen sie im Hintergrund (`&`).

**Empfehlung: systemd Service erstellen**

1. Frontend als systemd Service
2. Backend bereits als systemd Service vorhanden
3. Auto-Restart bei Server-Neustart

---

## ✅ Status:

- ✅ Frontend läuft (Port 3000)
- ✅ Backend läuft (Port 8001)
- ✅ Nginx Proxy konfiguriert
- ✅ Services sollten jetzt erreichbar sein!

**502 Error sollte jetzt behoben sein!** 🎯

